angular.module('gokibitz.directives')
.directive('sticky', function ($window, debounce) {
	return {
		restrict: 'A',
		link: function ($scope, elem, attrs) {
			function resize() {
			}

			function scroll() {
				lo
			}

			var $win = angular.element($window);

			$win.bind('scroll',
		}
	};
});
